<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thkim.playground">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Playground">
        <activity android:name=".recycler.RecyclerMainActivity"></activity>
        <activity android:name=".activity.ThreeActivity" />
        <activity android:name=".activity.TwoActivity" />
        <activity android:name=".activity.OneActivity" />
        <activity android:name=".chap02.course04.FindMeActivity" />
        <activity android:name=".chap02.course03.ClippingActivity" />
        <activity android:name=".chap02.course02.MiniPaint" />
        <activity android:name=".chap02.course01.CustomViewActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>